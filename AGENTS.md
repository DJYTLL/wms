# D:\project AGENTS.md

1. 凡涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 迁移脚本的修改，必须先扫描本地 migration 目录，确认当前最高版本号，再按顺序追加新的迁移文件；禁止跳号，禁止复用已有版本号，禁止修改、覆盖或重排历史迁移文件；除非我明确要求，否则一律只允许新增迁移，不允许直接改写既有迁移历史。开始修改前，先说明当前扫描到的最新版本号、拟新增的 migration 文件名，以及是否存在版本冲突。

2. 所有的问题中文回答。

3. 当新增页面，并且同时新增菜单、权限、列权限中的任意一种时，该任务默认视为“页面接入权限体系任务”，不是普通页面开发任务。

4. 新增页面时，必须同时检查并完成以下内容，否则任务不算完成：
   
   - 页面组件。
   - 前端路由，以及 route meta 中的 `title`、`permission`、`titleKey`。
   - 后端 permission seed、menu seed，以及需要的初始化数据或迁移。
   - 如页面涉及列权限，补齐 column 权限定义。
   - `RoleManagement.vue` 中的映射。
   - `PermissionManagement.vue` 中的映射。
   - `ColumnPermissionManagement.vue` 中的映射。

5. 新页面必须进入正确菜单分组，不能出现在“未映射页面”中。不要只做页面、路由、权限 seed 的一部分就结束。

6. 新增页面时，优先统一 `pageKey`、菜单 `code`、`i18nKey`、`path`、`permission_code`、权限前缀命名；不要为了临时兼容只补单点别名。

7. 对权限树、角色权限分配树、权限管理树、列权限树相关问题，优先检查是否错误使用“当前用户可见菜单”构树；默认应优先使用“完整菜单目录”构树。构树异常时，优先怀疑菜单数据源不完整，而不是单个页面漏映射。

8. 新增页面接入权限体系时，先补一个失败测试，锁住“该页面不会落入未映射页面”的行为；实现后必须跑相关测试和类型检查。

9. 交付时必须明确说明：
   
   - 新页面使用的 `pageKey`
   - 菜单 `code`
   - 路由 `name/path`
   - 权限 `code` 或 `prefix`
   - 在 `RoleManagement.vue`、`PermissionManagement.vue`、`ColumnPermissionManagement.vue` 中分别补了哪些映射
   - 为什么不会再落入“未映射页面”
10- 新增页面时，先列出“页面打开到首屏展示会触发的全部接口”，并逐一补齐权限、super_admin 旁路、菜单种子、历史库 migration 和回归测试；不允许只完成主页面路由和主列表接口。
